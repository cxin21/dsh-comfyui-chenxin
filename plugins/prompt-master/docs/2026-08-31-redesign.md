# Prompt Master DSH 插件 · 重新设计文档（2026-08-31）

> 触发：DSH 于 2026-08-31 更新（0.1.2-alpha.2 覆盖），用户要求重新调研全部 DSH 接口，
> 并结合提示词大师 3.1.0 源码 + anima-prompt-v1 + minimax-h3-prompt 技能源码重新设计插件。
> 状态：调研完成，方案待评审后实施。

---

## 1. DSH 接口调研结论（破坏性评估）

**结论：编译级无破坏，运行级 1 处已修复的 bug + 1 处需迁移的可移植性问题。**

| 服务 | 插件用法 | 0.1.2 契约 | 状态 |
|---|---|---|---|
| `ctx.llm` | `stream(GenerateOptions)` + `BlockAssembler` + `createUserMessage` | `stream(options): AsyncIterable<StreamChunk>`；`GenerateOptions{provider,model,messages,system,maxTokens,temperature,signal,...}` | ✅ 未变 |
| `ctx.tools` | `register(defineTool({...output, execute(args, exec)}))` | `ToolDefinition` = ToolSchema + output + `execute(args, exec: ToolRunContext)` | ✅ 未变（新增可选 `timeoutMs`/`isConcurrencySafe`/`finalizeContent`） |
| `ctx.settings` | `register(ns, schema, {base})` + `scope.get/update/replace` | 同左；`SettingsScope` 形状一致 | ✅ 未变 |
| `ctx.subagents` | `start(provider, {label, prompt, parent, signal})` → `run{result, dispose}` | `start(name, request): Promise<SubagentRun>`；provider `spawn`/`fork` 已注册 | ✅ 未变（实测成功） |
| `exec.agent`/`exec.signal` | 工具执行上下文拿调用 Agent + 取消信号 | `ToolExecutionInput.agent?` / `.signal` | ✅ 可用（实测） |

### 1.1 已修复的运行 bug
`prompt_author` 之前报 `SubagentIntentProvider 需要调用 Agent 上下文（ownerCtx.agent 不可用）`：
- **根因**：intent provider 在 plugin `apply()` 时绑定 `ownerCtx.agent`，但 loader 挂载上下文（preset scope）**没有** `agent` accessor（只有真正 Agent 的 ctx 才 shadow 它）。
- **修复**：`subagentIntent(req, exec?)` 改用工具执行时的 `exec.agent` 作为 parent（设计文档 §5.1 本就要求从 exec 拿），signal 用 `exec.signal`。
- **验证**：临时工具实测 `ctx.subagents.start('spawn', {parent: exec.agent})` 子代理返回 "OK"；270 测试全绿；`standingKeyFor` mount OK。

### 1.2 intent 层裁决（用户）
**intent 层保持走 DSH subagent 与大模型交互，不直接连 llm**。`defaultIntent`（direct llm）仅作 fallback。

---

## 2. 三份源码资产清单（调研产出）

详见 `temp/research/` 下三份报告 + 本文 §3 差距分析。

### 2.1 提示词大师 3.1.0（`temp/research/promptmaster-3.1.0.md`）
- **48 个内置 profile**：10 MiniMax H3 场景 + 13 反推镜像扩写 + 3 反推（Descriptive/SD/Danbooru）+ 12 训练打标 + 10 Torii
- 扩写 8 规则（natural/compact/cinematic/danbooru/photographer/descriptive_en/structured_md/structured_json）+ 三层注入（语言锁→篇幅→附加要求）
- 反推中枢：`buildSystemPrompt → Addons → OutputConstraints → UserTaskLead/Body/TailAddon → sanitizeFinalCaption`
- ComfyUI 还原检查表 9 类目 + 5 个 COMFYUI_EXTRA_TEMPLATES（含人物/风格 LoRA 训练模板）
- Anima3 §1–§5 槽位系统（含互斥速查/总量 band/决策树）
- **可移植性：约 40 个纯函数 + 60 双语文案常量可 1:1 移植为 TS**；不可移植的是 llamaRuntime/llamaContext/captionModels（本地 GGUF）、expandMedia 文件路径

### 2.2 anima-prompt-v1（`temp/research/anima-prompt-v1.md`）
- CLI 11 actions；4 阶段管线 Policy→Grounding→Composition→Inspection
- **实库 783 MiB**：tag-catalog.sqlite records 1,374,908 / names 1,388,017（文档示例 12,480 已过时）
- 落词 4 类：canonical/alias 替换为 prompt_form；fuzzy/miss 保留原文 + `catalog_miss` assumption
- **实测修正**：`silver hair` 是 fuzzy（silvery_hair 才 canonical）；gold 系同构
- 集成结论：编译/审计/互斥对/策略表全纯函数；catalog search 必须运行时读 sqlite（插件 `anima-catalog.ts` node:sqlite 直读是正确蓝本）

### 2.3 minimax-h3-prompt（`temp/research/minimax-h3-prompt.md`）
- 单 action `author`；`parse_request → build_text_pair → audit_prompt → build_budget`
- 方言：三字段版（t2va/i2va/fl2va/l2va）+ 六字段版（ref2va）；keyframe 三段对齐前言逐字
- audit 12 类硬闸门（7000 字符/字段顺序/时长/镜头编号/时间戳递增/切镜语法/语义冗余/`<d>`/声景分离/标签解析/前言/结构）
- tokenizer.json 7.03 MB（BPE 151K vocab）可 TS 直读；**官方 counter = 帧内全量 BPE**（`<|im_start|>user\n{vision pads}{text}<|im_end|>\n`）
- **唯一明显差距**：插件 `budget.ts` chat 帧底 `5 + 2N` 估算 → 应改 `countContext('', N)` 精确化

---

## 3. 现有实现差距分析（重新设计的核心输入）

### 已确认的差距（必须修）

| # | 差距 | 现状 | 修法 |
|---|---|---|---|
| G1 | **硬编码绝对路径** | `anima-catalog.ts` L25-26/157 与 `tokenizer-h3.ts` L17 硬编码 `C:/Users/11245/...` | 改为 preset 相对路径解析（loader 传 presetRoot 或环境变量）；随 preset 迁移可移植 |
| G2 | **render/ 层缺失** | 设计文档要求五层 intent/schema/dialect/audit/render，但 `src/pe-framework/render/` 不存在；P1 Envelope 组装 + target_slot_hint 散在 `prompt-author.ts` 的 `renderEnvelope` | 抽出 `render/` 层：`assembleEnvelope(stage, advisories)` + `targetSlotHint(target, stage)` 纯函数；prompt_compile/prompt_author/prompt_audit 共用 |
| G3 | **H3 budget chat 帧底估算** | `budget.ts` L76 `5 + 2N`（有引用时） | 改 `countTokensH3('', dir, N)` 精确帧底（官方 `countContext('', N)` 等价） |
| G4 | **intent 层签名** | 已修 exec.agent 但 `subagentIntent(req, exec?)` 的 exec 是 `any` | 用 `ExecLike`（route.ts 已有类型）收紧类型；child parent/signal/路由三处来源统一 |

### 可增强（非必须，设计评审后定）

| # | 增强 | 说明 |
|---|---|---|
| E1 | **提示词大师 3.1.0 资产核对** | 现有 resolver 是从 1:1 移植（expand 8 ids 完全一致），需逐文件核对 reverse/prose/torii/train 是否与 3.1.0 最新一致（旧 id 兼容分支勿删） |
| E2 | **工具新能力** | `catalog_search`/`profile_list` 加 `isConcurrencySafe: () => true`（并行工具调用）；耗时工具加 `timeoutMs` |
| E3 | **subagent structured output** | `ctx.subagents.start` 支持 `outputSchema` —— intent 子代理可直接产出校验 JSON（减少解析脆性），但需评估 persona/指令适配 |
| E4 | **catalog 路径注入** | `setCatalogPath` 已存在，但默认值硬编码 → 由 plugin config 或 loader 注入 presetRoot |
| E5 | **anima brief 全字段** | 现有 `AnimaSlots` 缺 `explicit`/`qualityPrefix`/`exclusions` 完整透传（composition.py 有这些），核对 brief 契约 |

### 明确不做（范围外）
- camera-* 执行端（loader 集成层，用户裁决暂不考虑）
- 训练打标专属工具（设计文档 §13 后议）
- JoyCaption 额外选项复活（需从原 PM 取实现文本）

---

## 4. 目标架构（重新设计后）

```
prompt-master 插件（plugins/prompt-master/）
├── plugin/
│   ├── index.ts        apply：注册 8 工具 + settings 命名空间 + intent provider 绑定（exec.agent 注入）
│   └── config.ts       Config{temperature} + 新增 presetRoot（G1 用）+ provider 名
├── llm/
│   ├── complete.ts     保留（reverse/expand 意图层 + fallback）
│   └── route.ts        保留（resolveRoute 三环）
├── pe-framework/
│   ├── intent/         subagent-provider（exec.agent + subagents.start；G4 收紧类型）
│   ├── schema/         h3-shots / scenes（保留）
│   ├── dialect/        anima（catalog 直读 + 编译）+ h3（方言渲染）
│   ├── audit/          rules-h3 + tokenizer-h3 + budget（G3 精确帧底）
│   ├── render/         【新增】assembleEnvelope + targetSlotHint（G2）
│   └── types.ts
├── resolver/           PE 引擎（提示词大师 1:1；E1 核对）
├── tools/              8 工具（E2 新能力；E5 brief 全字段）
└── tests/              fidelity 双跑保真（golden 维护）+ plugin + resolver + audit-loop
```

### 关键接口契约（设计定稿）

**intent（subagent）**：
```ts
// subagent-provider.ts
export function createSubagentIntentProvider(ownerCtx: Context, opts?): AuthorIntentFn
// AuthorIntentFn = (req, exec?: ExecLike) => Promise<AuthorDraft>
//  child = await ownerCtx.subagents.start(opts.provider ?? 'spawn', {
//    label, prompt: [{type:'text', text: taskText}], parent: exec?.agent, signal: exec?.signal })
//  result = await child.result  →  output text blocks → parseIntentJson → dispose()
```

**render 层（新增）**：
```ts
// pe-framework/render/index.ts
assembleEnvelope(stage: StageResult, trail: string[]): string  // P1 Envelope JSON
targetSlotHint(target: Target, stage: string): string           // anima→'t2i.prompt' h3→'t2v.prompt'
```

**resource 路径（G1 修复）**：
```ts
// 新增 pe-framework/runtime/env.ts
resolvePresetKnowledgeDir(): string
// 优先级：plugin config.presetRoot → env DSH_COMFYUI_PRESET_ROOT → 相对 loader 目录
// anima-catalog / tokenizer-h3 全部改走它，禁止硬编码
```

---

## 5. 实施路径（评审后分批）

| 批次 | 内容 | 验收 |
|---|---|---|
| R1 可移植性 | G1 路径注入 + G2 render 层 + G4 类型收紧 | tsc 干净；270 测试绿；删掉 C:/Users/11245 硬编码后在本机仍绿 |
| R2 保真增强 | G3 budget 精确帧底 + E5 anima brief 全字段 | 新增 golden 断言 chat 帧底与官方一致 |
| R3 资产核对 | E1 对照提示词大师 3.1.0 全 profile + E3/E4 评估 | resolver 与 3.1.0 逐项 diff；含新增（如有）测试 |
| R4 工具面 | E2 isConcurrencySafe + timeoutMs | 工具 schema 不变；并行调用测试绿 |
| R5 回归 | 全量测试 + 真实会话 prompt_author 端到端（subagent intent） | prompt_author anima/h3 产出 Envelope；修正闭环收敛 |

---

## 6. 决策记录

- D1（用户裁决）：intent 层走 subagent（`ctx.subagents.start` + `exec.agent`），不直接连 llm。
- D2：camera-*/loader 集成层本轮不动。
- D3：catalog 保持运行时 sqlite 直读（783 MiB 不可入内存）；node:sqlite readOnly 为蓝本。
- D4：H3 budget 精确化（G3）优先于 estimate 语义；estimate 仅作 tokenizer 缺失的显式回退。
- D5：render/ 层独立（G2），prompt_author/compile/audit 共用 Envelope 组装。

---

## 7. 参考资料

- 调研报告：`temp/research/{dsh-interface-audit,promptmaster-3.1.0,anima-prompt-v1,minimax-h3-prompt}.md`
- 提示词大师解包：`temp/pm-promptmaster-src/unpacked/electron/config/`
- 设计文档（插件内）：`plugins/prompt-master/docs/{architecture,2026-08-27-prompt-engineering-framework-design,2026-08-27-native-plugin-design,usage-prompt-engineering}.md`